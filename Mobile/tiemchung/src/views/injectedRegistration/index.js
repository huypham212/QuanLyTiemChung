import React from "react";
import styles from "./styles";
import { ScrollView } from "react-native";
import { Button, Text, InputField } from "../../components";

const injectedRegistration = () => {
    return (
        <ScrollView>
            <Text>ABC</Text>
        </ScrollView>
    )
}

export default injectedRegistration