import React from 'react';
import { ScrollView } from 'react-native';
import { Button, Text, InputField } from "../../components";

const Confirmation = () => {
    return (
        <ScrollView>
            <Text>Phiếu xác nhận</Text>
        </ScrollView>
    )
}

export default Confirmation