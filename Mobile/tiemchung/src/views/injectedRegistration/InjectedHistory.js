import React from 'react';
import { ScrollView } from 'react-native';
import { Button, Text, InputField } from "../../components";

const InjectedHistory = () => {
    return (
        <ScrollView>
            <Text>Tiền sử tiêm</Text>
        </ScrollView>
    )
}

export default InjectedHistory